@inject ImageViewerService ImageViewer

<div class="book-tile" @onclick="OnClick">
    @if (Book.PhotoUrl1 is { } cover)
    {
        <img class="book-tile-img zoomable" src="@cover" alt=""
             @onclick="() => ImageViewer.Show(cover)" @onclick:stopPropagation="true"/>
    }
    else
    {
        <img class="book-tile-img no-cover" src="images/istockphoto-1125976691-170667a.jpg" alt=""/>
    }
    <div class="book-tile-title">@Book.Title</div>
    @if (Book.LabelNames is { Count: > 0 })
    {
        <div class="book-labels">
            @foreach (var label in Book.LabelNames)
            {
                <span class="label-chip">@label</span>
            }
        </div>
    }
</div>
